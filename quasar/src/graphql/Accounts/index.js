import * as typeDefs from './typeDefs'
import * as queries from './queries'
import { resolvers } from './resolvers'
import * as mutations from './queries'

export {
  typeDefs,
  queries,
  resolvers,
  mutations
}
